package com.practice.code.collection;

/**
 * 要求：
 * 1、反转List集合的元素
 * 2、Set<Object>集合按，存入两个类型的对象User、Cat
 * 3、Set<Object>集合按，要求两种类型的对象User、Cat属性相同的对象，不能重复
 * 4、Map集合让Map有顺序，顺序为加入元素的顺序，并输出
 */
public class MapByOrder {
}
